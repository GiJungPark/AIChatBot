### Create .env file

해당 파일에는 아래와 같은 설정 값을 입력해야 합니다.

~~~
DATABASE_URL={데이터 베이스 주소}

DATABASE_USERNAME={데이터 베이스 사용자 이름}

DATABASE_PASSWORD={데이터 베이스 비밀번호}

GPT_API_KEY={OPEN AI API 키}

GPT_API_MODEL={사용하려는 모델}

JWT_KEY={32바이트 길이를 초과하는 Secret 키}

JWT_EXPIRE_TIME={JWT 만료 시간}
~~~